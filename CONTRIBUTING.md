# 別のPC・別ユーザーとの共同開発（コラボレーション）ガイド

別のパソコンや別のユーザーが、現在のリポジトリ（`bunkyo-web`）を共有してAntigravityで共同編集するためのセットアップと基本的なGitの操作手順です。

共同作業を行う方（別のPCのユーザー）に、この手順を共有してください。

---

## 0. 事前知識と準備（オーナー向け）

共同開発を始める前に、GitとGitHubの役割を理解し、適切な権限設定を行う必要があります。

### GitとGitHubの違い
* **Git**: PCにインストールして、自分の手元でファイルの変更履歴を記録・管理するための「ソフトウェア」です。
* **GitHub**: そのGitの記録をインターネット上のクラウドにアップロードして、複数人で共有・保存するための「Webサービス」です。

### リポジトリへの招待（権限付与）
共同編集者がリポジトリにアクセスし、コードをプッシュ（アップロード）できるようにするためには、リポジトリのオーナー（あなた）が以下の手順で招待を行う必要があります。

1. **共同編集者のアカウント作成**: 共同編集者にGitHubアカウントを作成してもらいます。
2. **GitHub上での招待**:
   - GitHubの `bunkyo-web` リポジトリページを開きます。
   - 上部のタブから **Settings**（歯車マーク）をクリックします。
   - 左側のメニューから **Collaborators** を選択します。
   - **Add people** ボタンをクリックし、共同作業者のGitHubユーザー名またはメールアドレスを入力して追加します。
3. **招待の承認**: 共同編集者に届く招待メール（またはGitHub上の通知）から **Accept invitation（招待を承認）** をクリックしてもらいます。

これで準備は完了です。以下の手順に進んでください。

---

## 1. 別のPCでの初期セットアップ（GitHubからの接続とダウンロード）

別のPCにはまだファイルが何もない状態ですので、GitHubからリポジトリを丸ごとダウンロード（クローン）するところから始めます。

### 手順
1. 新しいPCのターミナル（コマンドプロンプトやPowerShell）を開き、プロジェクトを保存したいフォルダに移動します。
2. 以下のコマンドを実行して、GitHubからリポジトリをダウンロードします。
   ```powershell
   git clone https://github.com/Marnackallen/bunkyo-web.git
   ```
3. ダウンロードが完了すると `bunkyo-web` というフォルダが作成されるので、そのフォルダに移動します。
   ```powershell
   cd bunkyo-web
   ```
4. **Antigravity IDEで開く**
   Antigravityを起動し、この `bunkyo-web` フォルダを開きます。これで共同編集の準備は完了です。

> [!TIP]
> **Gitがインストールされていない場合**
> 事前に [Git公式サイト](https://git-scm.com/) からGitをインストールしておく必要があります。

---

## 2. 共同編集の基本的なワークフロー

複数人で開発を行う場合、ファイルが上書きされたり先祖返りしたりするのを防ぐため、以下の**3つのステップ**を習慣づけることが重要です。

### ステップ1：作業を始める前に最新版を取得する（必ず行う！）
自分が作業を始める前に、他の人がプッシュした変更を取り込んでローカル環境を最新にします。
```powershell
git pull origin main
```

### ステップ2：ファイルの編集と保存
Antigravity上でファイルを編集し、通常通り保存します。

### ステップ3：自分の変更をGitHubに送信（プッシュ）する
作業が一段落したら、以下の3つのコマンドを実行して自分の変更を共有します。

```powershell
# 1. 変更したすべてのファイルを登録
git add .

# 2. 変更内容にメッセージをつけてセーブ（コミット）
git commit -m "〇〇の機能を追加"

# 3. GitHubのリポジトリに送信（プッシュ）
git push origin main
```

---

## 3. Gitの動作確認・状態チェック

今自分がどの状態にいるのかわからなくなった場合は、いつでも以下のコマンドで確認できます。

- **`git status`**
  現在の状態を確認します。（変更されたファイルはあるか、コミットすべきものがあるか等が表示されます）

- **`git log --oneline`**
  過去のコミット（セーブ）履歴を一覧で確認します。

> [!WARNING]
> **競合（コンフリクト）が発生した場合**
> もし自分と他の人が「全く同じファイルの同じ行」を同時に編集してプッシュしようとすると、Gitが「どちらを優先すべきかわからない」とエラー（コンフリクト）を出します。
> その場合は、Antigravity上でGitの機能を使ってどちらのコードを採用するか選択し、再度コミットする必要があります。
> よくわからないエラーが出た時は、無理に進めずにAntigravityに直接質問してください！
